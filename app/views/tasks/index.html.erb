<h1>Nuestras tares</h1>
<% @tasks.each do |task| %>
<p><input type="checkbox" <%= "checked" if task.completed %> disabled="disabled"><%= link_to task.title, task_path(task)%></p>
<% end %>
<%= link_to "+ Crear nueva tarea", newtask_path %>
