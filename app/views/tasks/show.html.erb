<h1> <%= @task.title %> </h1>
<p> <%= @task.details %> </p>
<p> completada : <%= @task.completed ? "Si" : "No" %> </p>
<%= link_to "editar", editask_path(@task) %> ||
<%= link_to "borrar", task_path(@task), method: :delete %> ||
<%= link_to "Volver al las tareas", tasks_path %>
